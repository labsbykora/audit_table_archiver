apiVersion: v1
kind: ServiceAccount
metadata:
  name: archiver-serviceaccount
  namespace: audit-archiver
  labels:
    app: audit-archiver
    component: serviceaccount
  annotations:
    # For AWS EKS with IRSA (IAM Roles for Service Accounts)
    # Uncomment and update with your IAM role ARN:
    # eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/archiver-role
    
    # For GKE with Workload Identity
    # Uncomment and update:
    # iam.gke.io/gcp-service-account: archiver@PROJECT_ID.iam.gserviceaccount.com

---
# RBAC: If you need additional permissions (usually not required for CronJobs)
# Uncomment if needed:
# apiVersion: rbac.authorization.k8s.io/v1
# kind: Role
# metadata:
#   name: archiver-role
#   namespace: audit-archiver
# rules:
#   - apiGroups: [""]
#     resources: ["configmaps", "secrets"]
#     verbs: ["get", "list"]
# ---
# apiVersion: rbac.authorization.k8s.io/v1
# kind: RoleBinding
# metadata:
#   name: archiver-rolebinding
#   namespace: audit-archiver
# roleRef:
#   apiGroup: rbac.authorization.k8s.io
#   kind: Role
#   name: archiver-role
# subjects:
#   - kind: ServiceAccount
#     name: archiver-serviceaccount
#     namespace: audit-archiver

