# Kustomization file for easy deployment
# Usage: kubectl apply -k k8s/

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: audit-archiver

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - configmap.yaml
  - secret.yaml
  - cronjob-archiver.yaml
  - cronjob-restore.yaml

# Uncomment to add image overrides
# images:
#   - name: ghcr.io/labsbykora/audit_table_archiver
#     newName: your-registry/your-image
#     newTag: v1.0.0

# Uncomment to add common labels
# commonLabels:
#   environment: production
#   team: platform

# Uncomment to add common annotations
# commonAnnotations:
#   managed-by: kustomize

